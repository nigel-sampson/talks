<UserControl
    x:Class="Demo.App.Views.Issue.ReactionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Demo.App.Views.Issue"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel>
        <TextBlock Text="{Binding Title}" Style="{StaticResource TitleTextBlockStyle}" />
        <TextBlock Text="{Binding Author}" Style="{StaticResource SubtitleTextBlockStyle}" />
        <TextBlock Text="{Binding Hearts}" Style="{StaticResource SubtitleTextBlockStyle}" />
    </StackPanel>
</UserControl>
