<Page
    x:Class="Demo.App.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Demo.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cm="using:Caliburn.Micro"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="24">
            <TextBox Header="Login" Text="{Binding Login, Mode=TwoWay}" />
            <PasswordBox Header="Password" Password="{Binding Password, Mode=TwoWay}" Margin="0,12" />
            <Button cm:Message.Attach="Save" Content="Save" />
        </StackPanel>
    </Grid>
</Page>



