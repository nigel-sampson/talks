<UserControl
    x:Class="Demo.App.Views.IssuesListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cm="using:Caliburn.Micro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView ItemsSource="{Binding Issues}" SelectedItem="{Binding SelectedIssue, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ContentControl cm:View.Model="{Binding}" cm:View.Context="{Binding ElementName=ViewSelector, Path=SelectedValue.Content}" Style="{StaticResource CompositionContentControlStyle}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ContentControl cm:View.Model="{Binding SelectedIssue}" cm:View.Context="DetailView" Style="{StaticResource CompositionContentControlStyle}" Grid.Row="0" Grid.Column="1" />
        
        <AppBar Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2">
            <StackPanel>
                <TextBlock Text="{Binding ElementName=ViewSelector, Path=SelectedValue}"></TextBlock>
            <ComboBox x:Name="ViewSelector" HorizontalAlignment="Right">
                <ComboBoxItem Content="MasterView" />
                <ComboBoxItem Content="ReactionsView" />
            </ComboBox>
            </StackPanel>
        </AppBar>
    </Grid>
</UserControl>























