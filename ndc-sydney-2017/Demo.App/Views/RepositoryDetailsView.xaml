<UserControl
    x:Class="Demo.App.Views.RepositoryDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Demo.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ScrollViewer>
        <StackPanel Margin="24,0,12,24">
            <TextBlock Text="{Binding Name}" Style="{ThemeResource HeaderTextBlockStyle}" />
            <TextBlock Text="{Binding Description}" Style="{ThemeResource SubheaderTextBlockStyle}" Margin="0,12" />
            <toolkit:MarkdownTextBlock Text="{Binding Readme}" LinkForeground="#FF92CAF4" />
        </StackPanel>
    </ScrollViewer>
</UserControl>

