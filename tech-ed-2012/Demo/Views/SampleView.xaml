<UserControl
    x:Class="Demo.Views.SampleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Width="294" Margin="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Background="{StaticResource MetroBlueBrush}" Margin="0,0,0,10" Width="40" Height="40">

        </Border>
        <StackPanel Grid.Column="1" Margin="10,-10,0,0">
            <TextBlock x:Name="Title" Style="{StaticResource BodyTextStyle}" TextWrapping="NoWrap"/>
            <TextBlock x:Name="Subtitle" Style="{StaticResource BodyTextStyle}" Foreground="{StaticResource ApplicationSecondaryForegroundThemeBrush}"/>
        </StackPanel>
    </Grid>
</UserControl>
