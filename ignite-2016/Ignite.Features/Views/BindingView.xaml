<Page
    x:Class="Ignite.Features.Views.BindingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ignite.Features.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock Text="{x:Bind ViewModel.Count, Mode=OneWay}" />
            <Button Content="+" Click="{x:Bind ViewModel.Increment}" />
            <Button Content="-" Click="{x:Bind ViewModel.Decrement}" />
            <TextBlock
                Text="{x:Bind Format(ViewModel.Count), Mode=OneWay}"
                Visibility="{x:Bind ViewModel.HighScore, Mode=OneWay}" />
        </StackPanel>
    </Grid>
</Page>















