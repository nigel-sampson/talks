<Page
    x:Class="Ignite.Features.Views.ConnectedAnimationsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ignite.Features.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Connected Animations" Style="{ThemeResource HeaderTextBlockStyle}"/>

        <Border Tapped="OnNavigate" Grid.Row="1" Margin="0,24,0,0" VerticalAlignment="Top">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Image x:Name="Wallpaper" Width="240" Source="/Assets/wallpaper.jpg"/>

                <TextBlock x:Name="Title" Text="Some Interesting Title" Grid.Column="1" Style="{ThemeResource SubheaderTextBlockStyle}"
                           Margin="24,0,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Page>
